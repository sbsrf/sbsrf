name: Release (macOS)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: '测试原因（选填）'
        required: false
        default: 'Manual test run on macOS'
  push:
    tags:
      - '*'

permissions:
  contents: write

jobs:
  build-on-macos:
    runs-on: macos-latest
    defaults:
      run:
        shell: zsh {0}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download and extract librime
        run: |
          LIBRIME_URL="https://github.com/rime/librime/releases/download/1.16.1/rime-de4700e-macOS-universal.tar.bz2"
          LIBRIME_ARCHIVE="librime/rime.tar.bz2"
          
          mkdir -p librime
          curl -L -o "$LIBRIME_ARCHIVE" "$LIBRIME_URL" || { echo "❌ 下载 librime 失败"; exit 1; }
          
          if [[ ! -f "$LIBRIME_ARCHIVE" ]]; then
              echo "❌ 下载的 librime 文件不存在"
              exit 1
          fi
          
          tar -xjf "$LIBRIME_ARCHIVE" -C librime/ --strip-components=2 || { echo "❌ 解压 librime 失败"; exit 1; }
          echo "✅ 已解压 librime 到: $PWD/librime"
          # 新增：打印 librime 目录文件，确认动态库存在
          echo "📋 librime 目录文件列表："
          ls -la $PWD/librime

      - name: Configure environment variables
        run: |
          # 定义绝对路径，避免相对路径问题
          LIBRIME_PATH="$PWD/librime"
          # 1. 写入 GITHUB_ENV（供后续步骤使用）
          echo "LIBRIME_PATH=$LIBRIME_PATH" >> $GITHUB_ENV
          # 2. 直接导出到当前 shell（供本步骤验证）
          export PATH="$LIBRIME_PATH:$PATH"
          export DYLD_LIBRARY_PATH="$LIBRIME_PATH:$DYLD_LIBRARY_PATH"
          
          echo "✅ 已配置环境变量："
          echo "  - LIBRIME_PATH: $LIBRIME_PATH"
          echo "  - PATH: $PATH"
          echo "  - DYLD_LIBRARY_PATH: $DYLD_LIBRARY_PATH"

      - name: Verify rime_deployer and dependencies
        run: |
          # 显式引用 GITHUB_ENV 中的变量，并重新导出
          export LIBRIME_PATH="${{ env.LIBRIME_PATH }}"
          export PATH="$LIBRIME_PATH:$PATH"
          export DYLD_LIBRARY_PATH="$LIBRIME_PATH:$DYLD_LIBRARY_PATH"
          
          if command -v rime_deployer &> /dev/null; then
              echo "✅ rime_deployer 命令可用"
              echo "📋 rime_deployer 路径：$(which rime_deployer)"
              # 检查动态库依赖
              if otool -L $(which rime_deployer) | grep -q librime; then
                  echo "✅ librime 动态库依赖正常"
                  # 提前测试动态库加载
                  DYLD_LIBRARY_PATH="$LIBRIME_PATH" rime_deployer -h >/dev/null 2>&1 && echo "✅ 动态库加载测试通过" || echo "❌ 动态库加载测试失败"
              else
                  echo "❌ rime_deployer 存在但未找到 librime 动态库"
                  ls -la $LIBRIME_PATH
                  exit 1
              fi
          else
              echo "❌ 无法找到 rime_deployer 命令"
              ls -la $LIBRIME_PATH
              exit 1
          fi

      - name: Build project
        run: |
          # 核心修复：显式引用 GITHUB_ENV 变量，并强制重新导出
          export LIBRIME_PATH="${{ env.LIBRIME_PATH }}"
          export PATH="$LIBRIME_PATH:$PATH"
          export DYLD_LIBRARY_PATH="$LIBRIME_PATH"  # 强制覆盖，避免为空
          
          # 完整调试日志
          echo "📋 调试：当前工作目录"
          pwd
          echo "📋 调试：当前目录完整文件列表"
          ls -la
          echo "📋 调试：sbxlm 目录内容"
          ls -la sbxlm/ 2>/dev/null || echo "sbxlm 目录不存在/为空"
          
          echo "🔄 移动 sbxlm 目录下的文件..."
          setopt null_glob
          mv -f sbxlm/* . 2>/dev/null || echo "⚠️ sbxlm 目录可能为空或不存在"
          unsetopt null_glob
          echo "📋 调试：移动文件后的目录内容"
          ls -la
          
          echo "📋 调试：rime_deployer 详细信息"
          which rime_deployer
          file $(which rime_deployer)
          otool -L $(which rime_deployer)
          echo "📋 调试：环境变量（确认生效）"
          echo "LIBRIME_PATH: $LIBRIME_PATH"
          echo "PATH: $PATH"
          echo "DYLD_LIBRARY_PATH: $DYLD_LIBRARY_PATH"  # 确认不为空
          
          # 改用临时目录构建
          RIME_TEMP_DIR="$PWD/tmp_rime"
          mkdir -p "$RIME_TEMP_DIR"
          echo "📋 调试：目标目录：$RIME_TEMP_DIR"
          
          echo "🔨 运行 rime_deployer --build..."
          set -x
          # 强制指定 DYLD_LIBRARY_PATH 执行，确保动态库加载
          DYLD_LIBRARY_PATH="$LIBRIME_PATH" rime_deployer --build -v . "$RIME_TEMP_DIR" 2>&1 | tee build_error.log
          BUILD_EXIT_CODE=$?
          set +x
          
          if [[ $BUILD_EXIT_CODE -ne 0 ]]; then
              echo "❌ 构建失败！详细日志："
              cat build_error.log
              exit 1
          fi
          echo "✅ 构建成功！"

      - name: Clean up unnecessary files
        run: |
          rm -rf bihua* zhlf* sbfc* *.extended.dict.yaml *2.schema.yaml user.yaml sbpy.base.dict.yaml sbpy.ext.dict.yaml sbpy.tencent.dict.yaml 2>/dev/null
          echo "✅ 文件清理完成"

      - name: Package build artifacts
        run: |
          zip -q -r sbsrf.zip build lua opencc *.yaml *.txt 2>/dev/null
          if [[ -f "sbsrf.zip" ]]; then
              echo "✅ 打包成功！文件位置：$PWD/sbsrf.zip"
              du -h sbsrf.zip
          else
              echo "⚠️  打包步骤未找到指定文件，未生成 sbsrf.zip"
          fi

      - name: Upload release artifact
        uses: softprops/action-gh-release@v1
        with:
          files: sbsrf.zip
          prerelease: ${{ github.event_name == 'workflow_dispatch' }}
          body: |
            **Build Environment**: macOS-latest
            **Trigger Type**: ${{ github.event_name == 'workflow_dispatch' && 'Manual' || 'Tag Push' }}
            ${{ github.event_name == 'workflow_dispatch' && format('**Reason**: {0}', github.event.inputs.reason) || '' }}
