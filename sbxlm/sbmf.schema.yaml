# Rime schema
# encoding: utf-8

__include: sbft.schema.yaml:/

schema:
  schema_id: sbmf
  name: "声笔魔飞"
  icon: "nothing.ico"
  description: |
    —— 隐藏了候选框的飞天, 强制盲打。只能在Windows下使用。

style:
  inline_preedit: true #如果连嵌入的首选都不想看，请把true改为false
  preedit_type: preview #若只想看首选的编码，请把preview改为composition，如果想看所有选项就改为preview_all
  layout:
    margin_y: -1